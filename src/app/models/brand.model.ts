//src\app\models\brand.model.ts:

export interface Brand {
    id?: string;                // Optional ID for the brand
    brand: string;             // Brand name
    image: string[];           // Array of URLs for brand images
}
